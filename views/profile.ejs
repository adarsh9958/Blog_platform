<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2 mb-5 text-sm inline-block" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl tracking-tighter"><span class="font-light">Hello</span>, <%= user.name %> 👋🏻</h3>
        <h5 class="mb-5">You can create a new post</h5>
        <form method="post" action="/post">
            <textarea placeholder="What's on your mind ?" class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md block" name="content"></textarea>
            <input class="mt-2 w-40 px-3 py-2 bg-blue-500 rounded-md block text-sm" type="submit" value="Create New Post">
        </form>
        <div class="Post mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% for(it of user.posts.reverse()){ %>
                    <div class="post mb-4 w-1/3 p-4 rounded-md bg-zinc-800 border-[1px] border-zinc-700">
                        <h4 class="text-blue-500 mb-2">@<%=user.username %></h4>
                        <p class="text-sm tracking-tight"><%= it.content %></p>
                        <small class="mt-2 inline-block"><%=it.likes.length %> likes</small>
                        <div class="btns flex mt-1 gap-4">
                            <a class="text-blue-500" href="/like/<%=it._id %>">
                                <%= it.likes.indexOf(user._id)===-1 ? "Like":"Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%=it._id %>">Edit</a>
                        </div>
                    </div>
                <%} %>
            </div>
        </div>
    </div>
</body>
</html>